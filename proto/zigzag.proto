
syntax = "proto3";

package zigzag;

import "google/protobuf/empty.proto";

service ZiggyBlockchain {
    rpc Mine (google.protobuf.Empty) returns (MineResponse);
}

message MineResponse {
    uint64 index = 1;
    uint64 time = 2;
    uint64 proof = 3;
    string previous_hash = 4;
}
